{
  "description": "Execute a shell command and return its output (stdout + stderr combined).\n- You MUST set workdir explicitly; never rely on the current process directory\n- Commands time out after 120s by default\n- Common uses: run EditMode tests, show git status, find asmdef references\n- Do NOT use bash for reading/writing/editing files — the read/write/edit tools accept absolute paths anywhere on disk (including outside the workspace) and avoid quoting pitfalls\n- python/pip use the Python runtime selected in Settings > General. Managed Python installs pip packages into the Locus data directory by default. Other Python distributions launched here (uvx, venv, conda) run with their own environment untouched\n- Use the description field to briefly describe what the command does\n- Prefer several small bash calls over one long && / set -e chain: each call reports its own exit code and output, a mid-chain failure can leave earlier side effects half-applied, and note `grep` exits 1 when nothing matches\n- Nothing persists between calls (env vars, cd, shell functions); pass state through files and set workdir per call. PATH and environment variables are refreshed from the system on every call — CLIs installed by a previous command are directly available without PATH tweaks\n- stdin is closed: a command that prompts for input fails or hangs until timeout. Set interactive=true ONLY when the user must type into a real terminal (login/auth wizards, password/2FA/device-code prompts, TTY-only CLIs); it opens a visible terminal window on the user's machine, output is NOT captured and only the exit code is returned\n- After an interactive command, verify the outcome with a non-interactive check instead of trusting the exit code; if it fails before the user can interact (e.g. exit code 127), rerun with interactive=false to capture the error, fix it, then retry interactively",
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "The shell command to execute"
      },
      "description": {
        "type": "string",
        "description": "Brief description of what this command does, e.g. 'Run EditMode tests', 'Show git status', 'Find asmdef references'"
      },
      "timeout": {
        "type": "integer",
        "description": "Timeout in milliseconds (default 120000, or 600000 when interactive=true)"
      },
      "interactive": {
        "type": "boolean",
        "description": "Run the command in a visible terminal window where the user types input directly (login wizards, password prompts, TTY-only CLIs). Output is not captured; only the exit code is returned. Default false"
      },
      "workdir": {
        "type": "string",
        "description": "Working directory to run the command in. Required."
      }
    },
    "required": ["command", "description", "workdir"]
  }
}
