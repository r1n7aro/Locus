{
  "description": "Search Design, Memory, Reference, and Skill sources, plus plan/ execution documents. Provide lexicalQuery or semanticQuery. Summary results use one line per document: <path> :: <summary>. Detailed results use a single-line header: --- <path> | lines <start>-<end> | summary <line> | body <line> --- followed by optional hit context.\n\nEach header gives a real file path and 1-based physical line range, including frontmatter. Use `read` with that path, offset, and limit. Workspace paths are relative; registered external sources use absolute paths. Result titles are omitted because they match filenames; ranking metadata is also omitted.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "lexicalQuery": {
        "type": "string",
        "description": "Optional lexical query for exact terms, titles, paths, identifiers, or short keyword combinations."
      },
      "semanticQuery": {
        "type": "string",
        "description": "Optional semantic query for intent- or meaning-based retrieval using natural language."
      },
      "limit": {
        "type": "integer",
        "description": "Maximum number of results to return (default: 5, max: 20).",
        "default": 5
      },
      "pathPrefix": {
        "type": "string",
        "description": "Optional type-prefixed directory path prefix. Valid roots: `design/`, `plan/`, `memory/`, `skill/`, and `reference/`. Use `skill/` for workflows, SOPs, slash-command skills, tool/package instructions, and reusable execution procedures. Examples: `skill/`, `skill/unity/`, `plan/`, `memory/`, `design/`, `reference/unity/`. Directory prefixes do not end with `.md`."
      },
      "includeSummary": {
        "type": "boolean",
        "description": "Whether to return each document on one line in `<path> :: <summary>` format. Summary whitespace is collapsed so every document occupies exactly one line. Hit context, line positions, and ranking metadata are omitted. When false, each result uses a single-line document header followed by optional hit context for targeted reads.",
        "default": false
      },
      "includeHitContext": {
        "type": "boolean",
        "description": "Whether to return source text nearest to each hit so its meaning can be checked directly. Used only when `includeSummary` is false. Frontmatter matches return only the decoded `summary` or `maintenanceRules` value; internal metadata is omitted. Context is split on punctuation and line boundaries and capped by `hitContextMaxChars`.",
        "default": true
      },
      "hitContextMaxChars": {
        "type": "integer",
        "description": "Maximum characters of hit context to return per result (default: 220, min: 80, max: 1000). Used only when `includeSummary` is false and `includeHitContext` is true.",
        "default": 220,
        "minimum": 80,
        "maximum": 1000
      }
    }
  }
}
