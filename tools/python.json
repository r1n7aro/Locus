{
  "description": "Run Python in the current checkout with the Locus Python SDK ready to use. The selected runtime from Settings > General is used, and `locus`, `project`, and `workspace_ref` are injected into an async body, so top-level `await` works.\n\nUse action=`help` when a workflow needs more SDK surface. Topics cover agents, sessions, tools, workspace services, and callbacks. Use `locus.list_agents()`, `locus.list_sessions()`, `locus.list_running_sessions()`, and `locus.list_tools()` for discovery; running sessions support `send_message()` for sourced cross-session input. Pass the injected `workspace_ref` to checkout-scoped Agent runs and tool calls. For action=`run`, set readonly=true only when the Python body and every SDK/tool call it makes are observation-only.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["run", "help"],
        "description": "Run Python code, or progressively load one SDK help topic. Default run.",
        "default": "run"
      },
      "code": {
        "type": "string",
        "description": "Python statements executed inside an async function. `locus`, `project`, and `workspace_ref` are already defined; top-level await is supported. Required for action=run."
      },
      "topic": {
        "type": "string",
        "enum": ["overview", "agents", "sessions", "tools", "unity", "callbacks"],
        "description": "SDK documentation topic returned by action=help. Default overview."
      },
      "description": {
        "type": "string",
        "description": "Briefly describe the Python workflow."
      },
      "readonly": {
        "type": "boolean",
        "description": "Whether the Python body and all nested SDK/tool calls only observe state. Required for action=run; help is always read-only."
      },
      "timeout": {
        "type": "integer",
        "description": "Timeout in milliseconds for sync execution. Default 120000; maximum 1800000. Background async/notify execution has no deadline."
      }
    },
    "required": ["action", "description"],
    "additionalProperties": false
  }
}
