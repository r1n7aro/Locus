{
  "description": "Apply pending C# edits to the running Unity Editor in ~1-2 seconds via method hot-patching — no script recompile, no domain reload, no state loss. Works in Edit Mode and Play Mode; ideal for the edit→observe loop while playing.\n\nUse after editing .cs files with the write/edit tools. Edits are classified automatically:\n- Hot (applied live): method/property/indexer/constructor BODY changes, async↔sync conversion, using-directive changes (the whole file re-binds), accessibility widening, new methods of ANY accessibility (callable from later edits and snippets), new types, new .cs files.\n- Hot after call-site verification: method signature/return-type/static changes, renames, member deletions, accessibility narrowing — verified by scanning compiled call sites; if calls exist OUTSIDE this edit batch the tool returns cold with the EXACT files to edit (then retry). Deleting a Unity message method (Update/...) stops its behavior immediately via an empty stub.\n- Cold (needs a real compile): field layout changes, base-type/attribute changes, generic method bodies, partial types, interface members, NEW Unity message methods, virtual-member signature changes, constructor surface changes, const or static-initializer changes. The tool reports the exact reasons and queues the files — then call unity_recompile.\n- Blind spots: reflection by name, SendMessage(string), UnityEvent serialized bindings cannot be verified — they keep calling old members until a real compile.\n\nDeterministic compile errors are returned directly; fix the code and call again. Hot patches persist until the next domain reload, and the edited files are already on disk, so a later unity_recompile (or any reload) converges to identical code permanently — call unity_recompile once you finish a burst of hot edits, before relying on serialization/Inspector changes, or whenever the tool tells you to.\n\nRequires: Unity hot reload + sidecar compiler enabled in settings, an up-to-date Locus Unity plugin, and Editor Code Optimization = Debug.",
  "parameters": {
    "type": "object",
    "properties": {
      "paths": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Optional .cs paths (absolute or project-relative) to hot reload. Default: every .cs file edited since the last recompile."
      }
    },
    "required": []
  }
}
