{
  "description": "Present a completed implementation plan for user approval while Plan mode is active. Write or edit the session plan file first; this tool reads it and takes no plan-content argument. Approval enables implementation; rejection keeps Plan mode active so the plan can be refined.\n\nResolve required decisions before submission. Use this tool for implementation approval. For research-only requests, return findings directly without requesting implementation approval.",
  "parameters": {
    "type": "object",
    "properties": {}
  }
}
