{
  "description": "Get the latest progress or final result of an async-mode task by id. Notify-mode tasks deliver their final result automatically, so use this only for an explicit interim progress check.",
  "parameters": {
    "type": "object",
    "properties": {
      "task_id": {
        "type": "string",
        "description": "The task id returned by a tool started with async='async' or async='notify'."
      }
    },
    "required": ["task_id"],
    "additionalProperties": false
  }
}
