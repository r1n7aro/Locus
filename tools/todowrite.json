{
  "description": "Maintain the current task's structured todo list. Use for complex work, multiple deliverables, or an explicit user request; skip trivial work and direct answers. Each call replaces the full list. Keep one task in_progress and update statuses as work completes.",
  "parameters": {
    "type": "object",
    "properties": {
      "todos": {
        "type": "array",
        "description": "The complete updated todo list (replaces existing list)",
        "items": {
          "type": "object",
          "properties": {
            "content": {
              "type": "string",
              "description": "Brief description of the task"
            },
            "status": {
              "type": "string",
              "enum": ["pending", "in_progress", "completed", "cancelled"],
              "description": "Current status"
            },
            "priority": {
              "type": "string",
              "enum": ["high", "medium", "low"],
              "description": "Priority level (default: medium)"
            }
          },
          "required": ["content", "status"]
        }
      }
    },
    "required": ["todos"]
  }
}
