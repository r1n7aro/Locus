{
  "description": "Creates a new file on the local filesystem and writes the provided content.\n\nUsage:\n- This tool only creates new files. It will reject any path that already exists.\n- Use the edit tool for any change to an existing file.\n- Parent directories will be created automatically if they don't exist.\n- In a registered writable knowledge directory, provide Markdown body content only. Locus generates the required frontmatter, updates the knowledge index, and reports every generated field plus the final content start line.\n- When C# code analysis and edit/write diagnostics are enabled, successful .cs writes append capped file-scope diagnostics automatically.",
  "parameters": {
    "type": "object",
    "properties": {
      "filePath": {
        "type": "string",
        "description": "Absolute or relative path to the file to write"
      },
      "content": {
        "type": "string",
        "description": "The full file content, or Markdown body only when creating a registered knowledge document"
      }
    },
    "required": ["filePath", "content"]
  }
}
