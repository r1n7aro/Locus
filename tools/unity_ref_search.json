{
  "description": "Query the Unity asset reference graph database to search for asset relationships in both directions.\n\nSupports two search directions:\n- 'references': Find which assets reference (use) the given asset (incoming edges). Answers: \"what uses this asset?\"\n- 'dependencies': Find which assets the given asset depends on (outgoing edges). Answers: \"what does this asset use?\"\n\nGiven an asset path, resolves its GUID and queries the pre-built reference graph (rescan from the UI if results look stale).\n\nReturns results grouped by asset path with detailed ref_path showing the exact location within the asset where the reference occurs, including the GameObject hierarchy path, component type, and field name (e.g. 'Root/Child/MonoBehaviour/m_Script').",
  "parameters": {
    "type": "object",
    "properties": {
      "asset_path": {
        "type": "string",
        "description": "Path to the asset file relative to the Unity project root (e.g. 'Assets/Prefabs/Player.prefab')"
      },
      "direction": {
        "type": "string",
        "enum": ["references", "dependencies"],
        "description": "Search direction. 'references' finds assets that reference this asset (incoming edges). 'dependencies' finds assets this asset depends on (outgoing edges)."
      },
      "max_depth": {
        "type": "integer",
        "description": "Maximum traversal depth. 1 = direct only (default). Values > 1 enable transitive search."
      },
      "type_filter": {
        "type": "string",
        "description": "Filter results by asset type. Supports '|' separated multiple types (OR logic). Valid types: scene, prefab, material, animation, controller, genericAsset, script, texture, audio, shader, model, otherYaml, metaOnly. Example: 'script|prefab' returns only scripts and prefabs."
      }
    },
    "required": ["asset_path", "direction"]
  }
}
