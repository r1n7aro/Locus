{
  "description": "Delete a unified knowledge document or directory by path. Returns a plain-text status line.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "kind": {
        "type": "string",
        "enum": ["document", "directory"],
        "description": "Whether the target is a knowledge document or a knowledge directory."
      },
      "path": {
        "type": "string",
        "description": "Current type-prefixed path, for example `reference/render-pipeline/urp-notes.md`."
      }
    },
    "required": ["kind", "path"]
  }
}
