{
  "description": "List Skill manifests currently discoverable by Locus, including project Skills, built-in app Skills, app-installed packages, plugin packages, and generic external Skills. Returns one plain-text line per Skill.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "source": {
        "type": "string",
        "enum": ["project", "app", "pluginApp", "pluginProject", "externalUser", "externalProject"],
        "description": "Optional source filter."
      }
    }
  }
}
