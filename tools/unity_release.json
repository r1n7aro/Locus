{
  "description": "Release this session's cooperative Editor lock after its last state-dependent call. Continue ordinary asset/code work outside the lock. Already-free locks succeed; another session's lock cannot be released. Run completion and cancellation also release owned locks.",
  "parameters": {
    "type": "object",
    "properties": {},
    "required": []
  }
}
