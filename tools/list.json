{
  "description": "List a directory as a bounded tree. Set path explicitly; use read for files. Entries ending in / are directories. include_files=false returns directories only; omitted entries are summarized with counts. Root-level generated/cache/log/build directories and .meta files are excluded.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Root directory to list. Required. Pass an absolute path or a path relative to the selected project root."
      },
      "depth": {
        "type": "integer",
        "description": "Maximum depth of recursion. 1 = only immediate children, 2 = children and grandchildren, etc. Default: 2.",
        "default": 2
      },
      "max_items": {
        "type": "integer",
        "description": "Maximum number of child entries to show per directory. Excess entries are summarized. Default: 500.",
        "default": 500
      },
      "max_total": {
        "type": "integer",
        "description": "Maximum total number of entries across all directories. When reached, recursion stops early. Default: 1000.",
        "default": 1000
      },
      "include_files": {
        "type": "boolean",
        "description": "If true, list files in addition to directories. Default: false (directories only).",
        "default": false
      }
    },
    "required": ["path"]
  }
}
